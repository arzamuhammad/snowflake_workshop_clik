USE ROLE ACCOUNTADMIN;
CREATE WAREHOUSE IF NOT EXISTS GEN2_SMALL WAREHOUSE_SIZE='SMALL' AUTO_SUSPEND=60 AUTO_RESUME=TRUE INITIALLY_SUSPENDED=TRUE;
USE WAREHOUSE GEN2_SMALL;
CREATE DATABASE IF NOT EXISTS CLIK_WORKSHOP2 COMMENT='CLIK full-day workshop: ML, dashboard, Cortex Agent';
USE DATABASE CLIK_WORKSHOP2;
USE SCHEMA PUBLIC;
CREATE STAGE IF NOT EXISTS RAW_DATA_STAGE DIRECTORY=(ENABLE=TRUE) ENCRYPTION=(TYPE='SNOWFLAKE_SSE');
CREATE STAGE IF NOT EXISTS ML_STAGE DIRECTORY=(ENABLE=TRUE) ENCRYPTION=(TYPE='SNOWFLAKE_SSE');
CREATE FILE FORMAT IF NOT EXISTS CSV_FF TYPE='CSV' FIELD_DELIMITER=',' SKIP_HEADER=1 FIELD_OPTIONALLY_ENCLOSED_BY='"' NULL_IF=('','NULL','null') EMPTY_FIELD_AS_NULL=TRUE COMPRESSION='AUTO';
SELECT CURRENT_DATABASE() AS db, CURRENT_SCHEMA() AS sch, CURRENT_WAREHOUSE() AS wh;
